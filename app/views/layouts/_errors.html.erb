<% if element.errors.any? %>
  <div class="row">
    <div class="errors col-md-4 col-md-offset-4">
      <ul class="no-list-style">
        <% element.errors.full_messages.each do |error| %>
          <li><%= error %></li>
        <% end %>
      </ul>
    </div>
  </div>
<% end %>